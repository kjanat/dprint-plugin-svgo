# GENERATED BY ./ci.generate.ts -- DO NOT DIRECTLY EDIT

name: "CI"
on:
  pull_request:
    branches:
      - "master"
    paths-ignore:
      - "site/**"
      - "*.md"
      - "LICENSE"
      - ".github/workflows/pages.yml"
      - ".github/workflows/check-updates.yml"
      - ".github/workflows/release.yml"
      - ".github/dependabot.yml"
      - ".config/**"
      - ".gitignore"
      - ".gitattributes"
      - ".zed/**"
  push:
    branches:
      - "master"
    tags:
      - "*"
    paths-ignore:
      - "site/**"
      - "*.md"
      - "LICENSE"
      - ".github/workflows/pages.yml"
      - ".github/workflows/check-updates.yml"
      - ".github/workflows/release.yml"
      - ".github/dependabot.yml"
      - ".config/**"
      - ".gitignore"
      - ".gitattributes"
      - ".zed/**"
permissions:
  contents: "read"
concurrency:
  group: "${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}"
  cancel-in-progress: true
jobs:
  check:
    name: "${{ matrix.config.target }}"
    if: "github.event_name == 'pull_request'"
    runs-on: "${{ matrix.config.os }}"
    strategy:
      matrix:
        config:
          - os: "macos-latest"
            run_tests: "true"
            cross: "false"
            target: "aarch64-apple-darwin"
          - os: "ubuntu-latest"
            run_tests: "true"
            cross: "false"
            target: "x86_64-unknown-linux-gnu"
    env:
      CARGO_INCREMENTAL: 0
      RUST_BACKTRACE: "full"
    steps:
      - uses: "actions/checkout@v6"
      - uses: "dsherret/rust-toolchain-file@v1"
      - name: "Cache cargo"
        uses: "Swatinem/rust-cache@v2"
        with:
          prefix-key: "v4-${{matrix.config.target}}"
          save-if: "${{ github.ref == 'refs/heads/master' }}"
      - name: "Clear stale v8 build cache"
        shell: "bash"
        run: "rm -rf target/*/build/v8-*"
      - uses: "denoland/setup-deno@v2"
      - uses: "extractions/setup-just@v3"
      - name: "Build JS"
        run: "just build"
      - name: "Setup Rust target"
        if: "matrix.config.target == 'x86_64-apple-darwin' || matrix.config.target == 'aarch64-apple-darwin'"
        run: "rustup target add \"${{matrix.config.target}}\""
      - name: "Install cross"
        if: "matrix.config.cross == 'true'"
        run: "cargo install cross --locked --git https://github.com/cross-rs/cross --rev f86fd03bb70b4c6802847c18087e21391498b0b4"
      - name: "Build (Debug)"
        if: "matrix.config.cross != 'true' && !startsWith(github.ref, 'refs/tags/')"
        run: "just ci-build-debug ${{matrix.config.target}}"
      - name: "Build (Release)"
        if: "matrix.config.cross != 'true' && startsWith(github.ref, 'refs/tags/')"
        run: "just ci-build-release ${{matrix.config.target}}"
      - name: "Build cross (Debug)"
        if: "matrix.config.cross == 'true' && !startsWith(github.ref, 'refs/tags/')"
        run: "just ci-cross-build-debug ${{matrix.config.target}}"
      - name: "Build cross (Release)"
        if: "matrix.config.cross == 'true' && startsWith(github.ref, 'refs/tags/')"
        run: "just ci-cross-build-release ${{matrix.config.target}}"
      - name: "Lint"
        if: "!startsWith(github.ref, 'refs/tags/') && matrix.config.target == 'x86_64-unknown-linux-gnu'"
        run: "just ci-lint"
      - name: "Test (Debug)"
        if: "matrix.config.run_tests == 'true' && !startsWith(github.ref, 'refs/tags/')"
        run: "just ci-test-debug"
      - name: "Test (Release)"
        if: "matrix.config.run_tests == 'true' && startsWith(github.ref, 'refs/tags/')"
        run: "just ci-test-release"
  build:
    name: "${{ matrix.config.target }}"
    if: "github.event_name == 'push'"
    runs-on: "${{ matrix.config.os }}"
    strategy:
      matrix:
        config:
          - os: "macos-latest"
            run_tests: "true"
            cross: "false"
            target: "x86_64-apple-darwin"
          - os: "macos-latest"
            run_tests: "true"
            cross: "false"
            target: "aarch64-apple-darwin"
          - os: "windows-latest"
            run_tests: "true"
            cross: "false"
            target: "x86_64-pc-windows-msvc"
          - os: "ubuntu-latest"
            run_tests: "true"
            cross: "false"
            target: "x86_64-unknown-linux-gnu"
          - os: "ubuntu-latest"
            run_tests: "false"
            cross: "true"
            target: "aarch64-unknown-linux-gnu"
    env:
      CARGO_INCREMENTAL: 0
      RUST_BACKTRACE: "full"
    steps:
      - uses: "actions/checkout@v6"
      - uses: "dsherret/rust-toolchain-file@v1"
      - name: "Cache cargo"
        uses: "Swatinem/rust-cache@v2"
        with:
          prefix-key: "v4-${{matrix.config.target}}"
          save-if: "${{ github.ref == 'refs/heads/master' }}"
      - name: "Clear stale v8 build cache"
        shell: "bash"
        run: "rm -rf target/*/build/v8-*"
      - uses: "denoland/setup-deno@v2"
      - uses: "extractions/setup-just@v3"
      - name: "Build JS"
        run: "just build"
      - name: "Setup Rust target"
        if: "matrix.config.target == 'x86_64-apple-darwin' || matrix.config.target == 'aarch64-apple-darwin'"
        run: "rustup target add \"${{matrix.config.target}}\""
      - name: "Install cross"
        if: "matrix.config.cross == 'true'"
        run: "cargo install cross --locked --git https://github.com/cross-rs/cross --rev f86fd03bb70b4c6802847c18087e21391498b0b4"
      - name: "Build (Debug)"
        if: "matrix.config.cross != 'true' && !startsWith(github.ref, 'refs/tags/')"
        run: "just ci-build-debug ${{matrix.config.target}}"
      - name: "Build (Release)"
        if: "matrix.config.cross != 'true' && startsWith(github.ref, 'refs/tags/')"
        run: "just ci-build-release ${{matrix.config.target}}"
      - name: "Build cross (Debug)"
        if: "matrix.config.cross == 'true' && !startsWith(github.ref, 'refs/tags/')"
        run: "just ci-cross-build-debug ${{matrix.config.target}}"
      - name: "Build cross (Release)"
        if: "matrix.config.cross == 'true' && startsWith(github.ref, 'refs/tags/')"
        run: "just ci-cross-build-release ${{matrix.config.target}}"
      - name: "Lint"
        if: "!startsWith(github.ref, 'refs/tags/') && matrix.config.target == 'x86_64-unknown-linux-gnu'"
        run: "just ci-lint"
      - name: "Test (Debug)"
        if: "matrix.config.run_tests == 'true' && !startsWith(github.ref, 'refs/tags/')"
        run: "just ci-test-debug"
      - name: "Test (Release)"
        if: "matrix.config.run_tests == 'true' && startsWith(github.ref, 'refs/tags/')"
        run: "just ci-test-release"
      - name: "Pre-release (x86_64-apple-darwin)"
        id: "pre_release_x86_64_apple_darwin"
        if: "matrix.config.target == 'x86_64-apple-darwin' && startsWith(github.ref, 'refs/tags/')"
        run: "zip -r dprint-plugin-svgo-x86_64-apple-darwin.zip dprint-plugin-svgo

          echo \"ZIP_CHECKSUM=$(shasum -a 256 dprint-plugin-svgo-x86_64-apple-darwin.zip | awk '{print $1}')\" >> \"$GITHUB_OUTPUT\""
        working-directory: "target/x86_64-apple-darwin/release"
      - name: "Pre-release (aarch64-apple-darwin)"
        id: "pre_release_aarch64_apple_darwin"
        if: "matrix.config.target == 'aarch64-apple-darwin' && startsWith(github.ref, 'refs/tags/')"
        run: "zip -r dprint-plugin-svgo-aarch64-apple-darwin.zip dprint-plugin-svgo

          echo \"ZIP_CHECKSUM=$(shasum -a 256 dprint-plugin-svgo-aarch64-apple-darwin.zip | awk '{print $1}')\" >> \"$GITHUB_OUTPUT\""
        working-directory: "target/aarch64-apple-darwin/release"
      - name: "Pre-release (x86_64-pc-windows-msvc)"
        id: "pre_release_x86_64_pc_windows_msvc"
        if: "matrix.config.target == 'x86_64-pc-windows-msvc' && startsWith(github.ref, 'refs/tags/')"
        run: "Compress-Archive -CompressionLevel Optimal -Force -Path target/x86_64-pc-windows-msvc/release/dprint-plugin-svgo.exe -DestinationPath target/x86_64-pc-windows-msvc/release/dprint-plugin-svgo-x86_64-pc-windows-msvc.zip

          $hash = (Get-FileHash -Algorithm SHA256 target/x86_64-pc-windows-msvc/release/dprint-plugin-svgo-x86_64-pc-windows-msvc.zip).Hash.ToLower()

          \"ZIP_CHECKSUM=$hash\" >> $env:GITHUB_OUTPUT"
        shell: "pwsh"
      - name: "Pre-release (x86_64-unknown-linux-gnu)"
        id: "pre_release_x86_64_unknown_linux_gnu"
        if: "matrix.config.target == 'x86_64-unknown-linux-gnu' && startsWith(github.ref, 'refs/tags/')"
        run: "zip -r dprint-plugin-svgo-x86_64-unknown-linux-gnu.zip dprint-plugin-svgo

          echo \"ZIP_CHECKSUM=$(shasum -a 256 dprint-plugin-svgo-x86_64-unknown-linux-gnu.zip | awk '{print $1}')\" >> \"$GITHUB_OUTPUT\""
        working-directory: "target/x86_64-unknown-linux-gnu/release"
      - name: "Pre-release (aarch64-unknown-linux-gnu)"
        id: "pre_release_aarch64_unknown_linux_gnu"
        if: "matrix.config.target == 'aarch64-unknown-linux-gnu' && startsWith(github.ref, 'refs/tags/')"
        run: "zip -r dprint-plugin-svgo-aarch64-unknown-linux-gnu.zip dprint-plugin-svgo

          echo \"ZIP_CHECKSUM=$(shasum -a 256 dprint-plugin-svgo-aarch64-unknown-linux-gnu.zip | awk '{print $1}')\" >> \"$GITHUB_OUTPUT\""
        working-directory: "target/aarch64-unknown-linux-gnu/release"
      - name: "Upload artifacts (x86_64-apple-darwin)"
        if: "matrix.config.target == 'x86_64-apple-darwin' && startsWith(github.ref, 'refs/tags/')"
        uses: "actions/upload-artifact@v7"
        with:
          name: "x86_64-apple-darwin-artifacts"
          path: "target/x86_64-apple-darwin/release/dprint-plugin-svgo-x86_64-apple-darwin.zip"
      - name: "Upload artifacts (aarch64-apple-darwin)"
        if: "matrix.config.target == 'aarch64-apple-darwin' && startsWith(github.ref, 'refs/tags/')"
        uses: "actions/upload-artifact@v7"
        with:
          name: "aarch64-apple-darwin-artifacts"
          path: "target/aarch64-apple-darwin/release/dprint-plugin-svgo-aarch64-apple-darwin.zip"
      - name: "Upload artifacts (x86_64-pc-windows-msvc)"
        if: "matrix.config.target == 'x86_64-pc-windows-msvc' && startsWith(github.ref, 'refs/tags/')"
        uses: "actions/upload-artifact@v7"
        with:
          name: "x86_64-pc-windows-msvc-artifacts"
          path: "target/x86_64-pc-windows-msvc/release/dprint-plugin-svgo-x86_64-pc-windows-msvc.zip"
      - name: "Upload artifacts (x86_64-unknown-linux-gnu)"
        if: "matrix.config.target == 'x86_64-unknown-linux-gnu' && startsWith(github.ref, 'refs/tags/')"
        uses: "actions/upload-artifact@v7"
        with:
          name: "x86_64-unknown-linux-gnu-artifacts"
          path: "target/x86_64-unknown-linux-gnu/release/dprint-plugin-svgo-x86_64-unknown-linux-gnu.zip"
      - name: "Upload artifacts (aarch64-unknown-linux-gnu)"
        if: "matrix.config.target == 'aarch64-unknown-linux-gnu' && startsWith(github.ref, 'refs/tags/')"
        uses: "actions/upload-artifact@v7"
        with:
          name: "aarch64-unknown-linux-gnu-artifacts"
          path: "target/aarch64-unknown-linux-gnu/release/dprint-plugin-svgo-aarch64-unknown-linux-gnu.zip"
      - name: "Generate schema"
        if: "matrix.config.target == 'x86_64-unknown-linux-gnu' && startsWith(github.ref, 'refs/tags/')"
        run: "just schema"
      - name: "Upload schema artifact"
        if: "matrix.config.target == 'x86_64-unknown-linux-gnu' && startsWith(github.ref, 'refs/tags/')"
        uses: "actions/upload-artifact@v7"
        with:
          name: "schema-artifacts"
          path: "schema.json"
  draft_release:
    name: "draft_release"
    if: "startsWith(github.ref, 'refs/tags/')"
    needs: "build"
    runs-on: "ubuntu-latest"
    permissions:
      contents: "write"
    steps:
      - name: "Checkout"
        uses: "actions/checkout@v6"
      - name: "Download artifacts"
        uses: "actions/download-artifact@v8"
      - uses: "denoland/setup-deno@v2"
      - uses: "extractions/setup-just@v3"
      - name: "Move downloaded artifacts to root directory"
        run: "mv x86_64-apple-darwin-artifacts/dprint-plugin-svgo-x86_64-apple-darwin.zip .

          mv aarch64-apple-darwin-artifacts/dprint-plugin-svgo-aarch64-apple-darwin.zip .

          mv x86_64-pc-windows-msvc-artifacts/dprint-plugin-svgo-x86_64-pc-windows-msvc.zip .

          mv x86_64-unknown-linux-gnu-artifacts/dprint-plugin-svgo-x86_64-unknown-linux-gnu.zip .

          mv aarch64-unknown-linux-gnu-artifacts/dprint-plugin-svgo-aarch64-unknown-linux-gnu.zip .

          mv schema-artifacts/schema.json ."
      - name: "Output checksums"
        run: "echo \"dprint-plugin-svgo-x86_64-apple-darwin.zip: $(shasum -a 256 dprint-plugin-svgo-x86_64-apple-darwin.zip | awk '{print $1}')\"

          echo \"dprint-plugin-svgo-aarch64-apple-darwin.zip: $(shasum -a 256 dprint-plugin-svgo-aarch64-apple-darwin.zip | awk '{print $1}')\"

          echo \"dprint-plugin-svgo-x86_64-pc-windows-msvc.zip: $(shasum -a 256 dprint-plugin-svgo-x86_64-pc-windows-msvc.zip | awk '{print $1}')\"

          echo \"dprint-plugin-svgo-x86_64-unknown-linux-gnu.zip: $(shasum -a 256 dprint-plugin-svgo-x86_64-unknown-linux-gnu.zip | awk '{print $1}')\"

          echo \"dprint-plugin-svgo-aarch64-unknown-linux-gnu.zip: $(shasum -a 256 dprint-plugin-svgo-aarch64-unknown-linux-gnu.zip | awk '{print $1}')\""
      - name: "Create plugin file"
        run: "just ci-create-plugin-file"
      - name: "Get svgo version"
        id: "get_svgo_version"
        run: "echo \"SVGO_VERSION=$(just ci-output-svgo-version)\" >> \"$GITHUB_OUTPUT\""
      - name: "Get tag version"
        id: "get_tag_version"
        run: "echo \"TAG_VERSION=${GITHUB_REF/refs\\/tags\\//}\" >> \"$GITHUB_OUTPUT\""
      - name: "Get plugin file checksum"
        id: "get_plugin_file_checksum"
        run: "echo \"CHECKSUM=$(shasum -a 256 plugin.json | awk '{print $1}')\" >> \"$GITHUB_OUTPUT\""
      - name: "Release"
        uses: "softprops/action-gh-release@v2"
        env:
          GITHUB_TOKEN: "${{ github.token }}"
        with:
          draft: true
          files: "dprint-plugin-svgo-x86_64-apple-darwin.zip

            dprint-plugin-svgo-aarch64-apple-darwin.zip

            dprint-plugin-svgo-x86_64-pc-windows-msvc.zip

            dprint-plugin-svgo-x86_64-unknown-linux-gnu.zip

            dprint-plugin-svgo-aarch64-unknown-linux-gnu.zip

            plugin.json

            schema.json"
          body: "# SVGO ${{ steps.get_svgo_version.outputs.SVGO_VERSION }}


            ## Install


            Dependencies:


            - Install dprint's CLI >= 0.40.0


            In a dprint configuration file:


            1. Specify the plugin url and checksum in the `\"plugins\"` array or run `dprint add kjanat/svgo`:


            \   ```jsonc

            \   {

            \     // etc...

            \     \"plugins\": [

            \       // ...add other dprint plugins here...

            \       \"https://github.com/kjanat/dprint-plugin-svgo/releases/download/${{ steps.get_tag_version.outputs.TAG_VERSION }}/plugin.json@${{ steps.get_plugin_file_checksum.outputs.CHECKSUM }}\"

            \     ]

            \   }

            \   ```


            2. Add a `\"svgo\"` configuration property if desired.


            \   ```jsonc

            \   {

            \     // ...etc...

            \     \"svgo\": {

            \       \"multipass\": true,

            \       \"pretty\": true,

            \       \"indent\": 2

            \     }

            \   }

            \   ```\n"
