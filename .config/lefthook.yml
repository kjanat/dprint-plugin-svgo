pre-commit:
  parallel: true
  jobs:
    - name: dprint fmt
      run: dprint fmt --staged
      stage_fixed: true

    - name: cargo clippy
      glob: "*.rs"
      run: just check-clippy

    - name: deno check
      glob: "*.{ts,js}"
      run: just check-deno

pre-push:
  jobs:
    - name: test
      run: just test

    - name: cargo build release
      run: cargo build --release
